import 'package:flutter/material.dart';

import '../widgets/student_tab_placeholder.dart';

class StudentDiscoveryPage extends StatelessWidget {
  const StudentDiscoveryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const StudentTabPlaceholder(title: 'Discovery');
  }
}
